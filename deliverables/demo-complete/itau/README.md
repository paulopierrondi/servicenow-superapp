# Itaú Complete Demo

This HyperFrames project renders the complete Itaú narrative with two personas:

- Executive user: opens the bank-like super app, asks the ServiceNow concierge for the day, reviews CMDB/incident/change evidence and approves the governed step.
- Fulfiller user: receives the same context, triages P0, validates CMDB Health and prepares ITSM/CSM/CRM actions.

Render from the repo root:

```bash
node scripts/render-complete-demo-videos.mjs
```

Output:

`deliverables/itau-demo/itau-servicenow-superapp-complete-demo.mp4`
